console.log(`data types objects`);

const person = {
  first: 'Paulo',
  last: 'Azoia',
  age: 100,
};

console.log(
  `Hello my name is ${person.first} ${person.last}, and I'm ${person.age} years old!!!`
);
